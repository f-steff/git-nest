{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/f-steff/git-nest/schemas/git-nest-output-v1.schema.json",
  "title": "git-nest command output v1",
  "type": "object",
  "required": ["version", "command", "recursive", "ok", "subprojects", "errors", "warnings"],
  "additionalProperties": false,
  "properties": {
    "version": { "const": 1 },
    "command": {
      "type": "string",
      "enum": ["status", "verify", "outdated", "diff", "foreach-modified", "foreach-clean", "doctor"]
    },
    "dry_run": { "type": "boolean" },
    "recursive": { "type": "boolean" },
    "ok": { "type": "boolean" },
    "subprojects": {
      "type": "array",
      "items": { "$ref": "#/$defs/row" }
    },
    "errors": {
      "type": "array",
      "items": { "type": "string" }
    },
    "warnings": {
      "type": "array",
      "items": { "type": "string" }
    },
    "checks": {
      "type": "array",
      "items": { "$ref": "#/$defs/check" }
    }
  },
  "$defs": {
    "check": {
      "type": "object",
      "required": ["code", "name", "status", "detail"],
      "additionalProperties": false,
      "properties": {
        "code": { "type": "string", "enum": ["I", "W", "E"] },
        "name": { "type": "string" },
        "status": { "type": "string", "enum": ["info", "warn", "error"] },
        "detail": { "type": "string" }
      }
    },
    "row": {
      "type": "object",
      "required": ["code", "path", "state", "target", "current", "expected", "detail"],
      "additionalProperties": false,
      "properties": {
        "code": { "type": "string", "minLength": 1, "maxLength": 1 },
        "path": { "type": "string" },
        "state": { "type": "string" },
        "target": { "type": "string" },
        "current": { "type": "string" },
        "expected": { "type": "string" },
        "detail": { "type": "string" }
      }
    }
  }
}
