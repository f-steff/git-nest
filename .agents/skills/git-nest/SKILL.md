---
name: git-nest
description: Use git-nest in an application or firmware workspace that consumes the tool. Use when an AI agent needs to inspect, restore, verify, edit, test, or prepare changes in a project with a .gitnest manifest and nested subprojects, while avoiding modifications to the git-nest tool itself.
---

# git-nest (pointer)

This is a development-agent pointer. The authoritative git-nest usage skill lives
in the product skill tree and is the single source of truth:

**Read `skills/git-nest/SKILL.md` (relative to the repository root) now and follow it.**

Do not edit this pointer to add workflow guidance. Update `skills/git-nest/SKILL.md`
instead. If you change the skill's purpose, keep the `description` above matching the
`description` in `skills/git-nest/SKILL.md` so discovery stays consistent.
